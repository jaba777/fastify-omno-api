# Fastify API Project

## Overview

This project is a Fastify-based API that integrates with the Omno API. It includes routes for creating transactions and handling webhook data.

## Running the Application

### Local Development

1. **Clone the Repository**

   ```bash
   git clone <repository-url>
   cd <repository-directory>
   ```

2. **Install Dependencies**
   npm install

3. **Set Up Environment Variables**

APP_URL=http://abcdefg.ngrok.io -> http://localhost:4000 APP_URL must be address of ngrok
CLIENT_ID=<your-client-id>
CLIENT_SECRET=<your-client-secret>
PORT=4000

4. **Run the Application**
   npm run dev

5. **Docker**
   Build and Run the Docker Container "docker-compose up --build"
